feat(security): autonomous lifecycle purge wired into auto-heal background worker

Wire purgeOldEntries() into the existing 5-minute auto-heal background
timer, transforming retention from a manual-only API into a zero-touch
background utility. Every tick, after chain healing and re-keying
complete, the worker now sweeps all orgs and purges expired entries
per their active retention policies.

audit-logger.cjs (updated):
- runAutonomousLifecyclePurge(): New function that iterates all orgs
  from getAllOrgIds(), calls purgeOldEntries(orgId) for each, and
  writes an audit_retention_auto_purge log entry when entries are
  evicted. Uses system actor (actorId: 'system', actorEmail:
  'system@internal'). Returns summary { totalPurged, totalArchived,
  orgsProcessed, orgsPurged, errors }.
- getLifecyclePurgeStats(): Returns _lifecyclePurgeStats snapshot
  (totalSweeps, totalPurged, totalArchived, lastResult, lastRun).
- _lifecyclePurgeRunning: Guard flag prevents concurrent sweeps.
- Timer tick order: healAllOrgs → runAutonomousReKeying →
  runAutonomousLifecyclePurge. Purge runs LAST to operate on a
  healed, re-keyed chain.
- Sequential for...of loop (not Promise.all) — matches existing
  healAllOrgs() pattern, avoids file lock contention.
- Per-org try/catch isolation — one org's failure doesn't block
  others. Errors recorded in errors array with orgId + message.
- Only logs when purged > 0 — avoids audit log spam every 5 minutes.
- Both functions exported in module.exports.

audit-logger-auto-purge.test.cjs (new):
- 18 tests covering: empty store, multi-org iteration, entry
  eviction, auto-purge log entry (action, actor, metadata, entity),
  no-log-when-zero-purge, error isolation, error recording, safety
  floor, hash chain validity, org boundary isolation, empty-after-
  purge, stats tracking, concurrent sweep guard, archive mode.

Security:
- Auto-purge log entry uses system actor (not a real user)
- Auto-purge log entry is PII-scrubbed via log() → scrubAuditEntry()
- Per-org error isolation prevents cascade failures
- Guard flag prevents concurrent sweeps
- Hash chain re-linked after purge (verifyChain passes)
- Safety floor preserves maxEntries most recent entries
- No cross-org purge (org boundary isolation)
- No new dependencies (Broom strategy — all inline)

CI workflow: added path filters and test regex for
audit-logger-auto-purge.

QA Framework: Builder/Validator separation followed.
- Test plan: .simplebeacon/qa/test_plan.md (17 check-items)
- Health report: .simplebeacon/qa/software_health_report.md
- 17/17 test plan items PASS
- Level 1: PASS (syntax, 1797 tests, gate 0/0/0, quality 100)
- Level 3: No spec drift, no ghost files
- Verdict: READY FOR COMMIT

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
