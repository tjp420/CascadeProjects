feat(security): expose auto-purge worker metrics in stats API and dashboard

Extend GET /api/audit/retention/stats to include autoPurgeStats (global
worker telemetry) and surface it in the frontend retention card as a
"Background Worker Activity" section. This completes the visibility
loop for the autonomous lifecycle purge worker, giving admins real-time
insight into background sweep activity without checking server logs.

audit-logger.cjs (user-modified + validator fixes):
- runAutonomousLifecyclePurge() refactored to async with Promise.resolve()
  wrappers, enabling both sync and async injected functions
- __testInject() added for unit test mocking (getAllOrgIds, purgeOldEntries,
  log)
- _lifecyclePurgeStats.failed counter added (tracks cumulative errors)
- getLifecyclePurgeStats() returns user-friendly aliases (runs, purged,
  archived, failed, lastRun) alongside legacy names
- Timer tick fixed to handle async promise rejection via .catch()

audit-routes.cjs (user-modified):
- GET /retention/stats now includes autoPurgeStats in response
- Defensive try/catch around getLifecyclePurgeStats() call
- Existing per-org stats fields unchanged (autoPurgeStats is additive)

SecurityView.js (updated):
- New "Background Worker Activity" card between stats grid and policy form
- 4-column grid: Total Sweeps, Auto-Purged, Auto-Archived, Last Sweep
- Last Sweep shows human-readable timestamp or "Never" when null
- Red border indicator on Last Sweep when failed > 0
- Warning message shown when failed > 0
- All dynamic fields escaped with escapeHtml()

audit-logger-auto-purge.test.cjs (updated):
- All 18 tests converted to async (await runAutonomousLifecyclePurge())
- All it() callbacks marked async

audit-lifecycle-worker.test.cjs (user-created, validator fixed):
- 4 tests using __testInject() for isolated orchestration testing
- Fixed shim compatibility (converted from node:test test() to
  describe/it pattern matching codebase convention)
- Tests: exports lifecycle API, background sweep, safety floor,
  thread loop isolation

Security:
- Endpoint still wrapped with authorize('admin:all')
- All dynamic frontend fields escaped with escapeHtml()
- No new dependencies (Broom strategy — all inline)

CI workflow: added audit-lifecycle-worker to path filters and test regex.

QA Framework: Builder/Validator separation followed.
- Test plan: .simplebeacon/qa/test_plan.md (11 check-items)
- Health report: .simplebeacon/qa/software_health_report.md
- 11/11 test plan items PASS
- Level 1: PASS (syntax, 1801 tests, gate 0/0/0, quality 100)
- Level 3: No spec drift, no ghost files
- Verdict: READY FOR COMMIT

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
