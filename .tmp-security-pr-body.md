## Summary
- New `SecurityAdminView.tsx` — a dedicated React admin panel wiring the `/status`, `/verify`, and `/chain/heal` endpoints into a live dashboard for system operators
- 4 KPI cards: Chain Integrity, Total Entries, Auto-Healed count, Poll Interval
- Per-org chain verification table with color-coded status (valid/healed/broken) and manual "Heal" buttons on broken chains
- "Verify Now" button for immediate out-of-band verification cycles
- Auto-refresh toggle (15s polling) with monitor status badges (Active/Stopped, Auto-Heal On/Off)
- Guardrail anomaly monitoring section with per-org blocked counts and spike detection
- Gated by `admin:all` permission via PermissionGuard at the routing layer
- Lazy-loaded with code-splitting; "Security Ops" nav item added to Sidebar Compliance group

#### Test plan
- [x] Build passes (TypeScript + Vite, zero errors)
- [x] Gate scan passes (0 blocking issues)
- [ ] Manual: verify Security Ops nav item appears for admin users
- [ ] Manual: verify KPI cards render with live data from /api/audit/security/status
- [ ] Manual: verify "Verify Now" button triggers POST /api/audit/security/verify
- [ ] Manual: verify "Heal" button on broken chains triggers POST /api/audit/chain/heal
- [ ] Manual: verify non-admin users cannot access the view (403 via PermissionGuard)

Generated with [Devin](https://devin.ai)
