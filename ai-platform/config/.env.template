# DEPRECATED — use ../../.env.example (canonical) instead of this legacy template.

# Environment Configuration Template
# Copy ../../.env.example to ../../.env and update values for your environment.

API_HOST=localhost
API_PORT=8081
DASHBOARD_HOST=localhost
DASHBOARD_PORT=8000
LOG_LEVEL=info
