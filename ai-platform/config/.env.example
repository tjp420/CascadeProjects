# DEPRECATED — legacy FastAPI/docker config folder.
#
# Canonical environment templates (use these instead):
#   ../.env.example              — local / dev (copy to ../.env)
#   ../.env.production.example   — production overlay
#   ../.env.v1-internal.example  — v1 dashboard profile (port 54355)
#
# See config/README.md for the env file map.
