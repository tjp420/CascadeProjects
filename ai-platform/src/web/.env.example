# DEPRECATED — use the canonical template at the repository root:
#   copy ../../../.env.example to ../../../.env
#
# Legacy web mirror keys retained for reference only.

API_HOST=localhost
API_PORT=8081
DASHBOARD_HOST=localhost
DASHBOARD_PORT=8000
LOG_LEVEL=info
