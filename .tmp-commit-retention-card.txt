feat(security): frontend retention dashboard card with purge confirmation modal

Add an audit retention management card to the SecurityView dashboard,
completing the security operations console quadfecta (interdiction +
key rotation + quarantine inspector + retention). The card provides
live retention stats, per-org policy configuration, and a
warning-gated manual purge trigger with two-step confirmation.

retentionService.js (new):
- fetchRetentionConfig(authHeaders): GET /api/audit/retention/config
- updateRetentionConfig(config, authHeaders): PUT
  /api/audit/retention/config
- fetchRetentionStats(authHeaders): GET /api/audit/retention/stats
- triggerRetentionPurge(authHeaders): POST /api/audit/retention/purge
- Follows existing service pattern (apiBase from authService.js,
  authHeaders parameter, credentials: 'include')

SecurityView.js (updated):
- renderRetentionSection(): Admin-only section placed at BOTTOM of
  admin panels (after quarantine inspector). Renders:
  - 4-card stats grid: Total Entries, Purgeable Count, Oldest Entry,
    Newest Entry
  - Policy form: Retention Days (>= 1), Max Entries (>= 100),
    Archive toggle, Save Policy button
  - Purge warning card: Shows purgeable count, retention period,
    warning border, Execute Purge button (disabled when 0 purgeable)
  - Two-step confirmation modal: Shows exact purge count, archive
    status, Cancel + Confirm Purge buttons
  - Empty/loading/error states with retry button
- loadRetention(): Fetches config and stats in parallel via
  Promise.all()
- handleSavePolicy(): Client-side validation (retentionDays >= 1,
  maxEntries >= 100), calls updateConfig(), shows toast, refreshes
- showPurgeConfirmation(): Shows modal dialog
- cancelPurge(): Hides modal without executing
- handlePurge(): Calls triggerRetentionPurge(), shows toast with
  purged/archived/remaining counts, refreshes stats
- All event listeners wired in render() method

Security:
- Section only renders for admin users (isCurrentUserAdmin check)
- All entry fields escaped with escapeHtml()
- Client-side validation matches backend (retentionDays >= 1,
  maxEntries >= 100)
- Two-step purge confirmation prevents accidental data loss
- Purge button disabled when 0 purgeable entries
- No new dependencies (vanilla JS, existing fetch API)

QA Framework: Builder/Validator separation followed.
- Test plan: .simplebeacon/qa/test_plan.md (18 check-items)
- Health report: .simplebeacon/qa/software_health_report.md
- 18/18 test plan items PASS
- Level 1: PASS (syntax, 1779 tests, gate 0/0/0, quality 100)
- Level 3: No spec drift, no ghost files
- Verdict: READY FOR COMMIT

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
