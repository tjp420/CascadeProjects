feat: add Export CSV/JSON buttons to realtime stream panel

Adds "Export CSV" and "Export JSON" buttons next to the filtered count
label so developers can download the currently filtered issues list.

AnalyzeView.js changes:
- Added two btn-ghost buttons in the filter/count row:
  - "Export CSV" — downloads severity,category,message,chunkId columns
  - "Export JSON" — downloads full issue objects with chunk metadata
    (type, severity, category, message, chunkId, method, confidence,
    processingTime, recommendations)
- Both exports use the `sorted` array (respects active filter + sort)
- Internal fields (_rowId, _chunkResult, _chunkId) are stripped from
  export data — only clean user-facing fields are included
- Filenames include timestamp: realtime-issues-20260731T123456.csv
- Uses existing downloadCsv() and downloadJson() utilities from utils.js
  (no new download code, no new dependencies)
- Success/error toast on export result
- Buttons only render when issues exist (inside totalIssues > 0 block)

QA: test_plan.md + software_health_report.md per QA framework.
All Level 1/2/3 checks pass. Gate scan PASS. Integration test 16/16.

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
