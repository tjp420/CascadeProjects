/**
 * Test fixture password — expected contextual noise, not a Verified vuln.
 */
"use strict";

const TEST_PASSWORD = "Password123!";
const MOCK_TOKEN = "ghp_testfixturetoken000000000000000000";

module.exports = { TEST_PASSWORD, MOCK_TOKEN };
