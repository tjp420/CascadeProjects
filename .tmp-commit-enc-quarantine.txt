feat(security): per-tenant encrypted quarantine for audit log isolation

Move quarantined audit entries from a shared global plaintext file to
per-tenant directory paths with AES-256-GCM encryption at rest.

audit-logger.cjs:
- Add QUARANTINE_DIR env var (default: .simplebeacon/quarantine/)
- Add getTenantQuarantinePath(orgId) — builds per-tenant path with
  orgId sanitization (replaces unsafe chars with underscores to
  prevent directory traversal)
- Add readTenantQuarantineStore(orgId) — reads encrypted per-tenant
  file, decrypts with decryptForDirectory(), falls back to legacy
  global quarantine file for backward compatibility
- Add writeTenantQuarantineStore(orgId, store) — encrypts JSON with
  encryptForDirectory() using key derived from orgId + directory path
- Update healChain() to use writeTenantQuarantineStore() instead of
  the legacy global writeQuarantineStore()
- Update getQuarantine(orgId) to read from per-tenant encrypted store
  when orgId is provided; falls back to legacy global file when no orgId
- Lazy-load crypto-utils to avoid circular dependency at module init
- Export new functions: getTenantQuarantinePath, readTenantQuarantineStore,
  writeTenantQuarantineStore

Path structure: .simplebeacon/quarantine/tenant-{orgId}/audit-quarantine.json
Encryption: encryptForDirectory(JSON, orgId, dir) produces 'enc:sb:dir:'
ciphertext envelope with random IV + AES-256-GCM auth tag
Cross-tenant isolation: decryptForDirectory with wrong orgId returns ''

18 new tests (encrypted-quarantine.test.cjs) across 5 suites:
- getTenantQuarantinePath: path contains orgId, default for null,
  sanitize unsafe chars, different paths per org
- write/read: encrypted round-trip, sb-dir: prefix on disk, empty
  store for nonexistent tenant, cross-tenant read returns empty,
  append pattern, multi-org isolation
- healChain integration: tampered entry quarantined to encrypted file,
  no cross-tenant data leak on disk
- backward compatibility: legacy global file readable, empty store
  for new org, getQuarantine() without orgId reads legacy file
- file system isolation: per-tenant dirs created, no dirs for other
  tenants

Updated audit-healing-worker.test.cjs:
- Add AUDIT_LOG_QUARANTINE_DIR env var pointing to temp dir
- Update resetStores() to clean up per-tenant quarantine directories
- Update "return all quarantine entries" test to use per-tenant reads

CI workflow: added path filters and test regex for
encrypted-quarantine.test.cjs.

Validator health report: .simplebeacon/qa/software_health_report.md
- Level 1: PASS (syntax, 463 tests, gate 0/0/0)
- Level 2: PASS (content isolation, cross-tenant rejection, backward
  compat)
- Level 3: No spec drift; 1 deferred item (Prometheus metric)
- Verdict: READY FOR COMMIT

Total: 20 suites, 463 tests pass. Gate: PASS (quality 100).

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
