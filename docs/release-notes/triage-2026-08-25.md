# SimpleBeacon Finding Triage — 2026-08-25

## Overview

- **Total warnings:** 10,956 (1,463 medium, 9,493 low)
- **Gate status:** PASSED (0 critical, 0 high — non-blocking only)
- **Scan:** `npx simplebeacon scan --full --gate` on `main` @ `2026-08-25T07:14:10Z`

## Distribution by location

| Location                                     | Count | Actionable?                              |
| -------------------------------------------- | ----- | ---------------------------------------- |
| `ai-platform/`                               | 4,899 | Yes — our code                           |
| `simplebeacon-vscode-merged/`                | 3,783 | Yes — our code                           |
| `node_modules_host/`                         | 1,031 | No — third-party deps, exclude from scan |
| `other/` (root scripts, worker-deploy, etc.) | 524   | Partially                                |
| `packages/`                                  | 692   | Yes — our code                           |
| `ai-agent/`                                  | 27    | Yes — our code                           |
| `coming-soon/`                               | 0     | Clean                                    |

## Triage by rule (own code only, 9,925 findings)

### SB-AI-002: TODO/FIXME/HACK marker (5,316 low)

**Status:** Low priority — mostly legitimate TODOs in active codebase
**Top files:**

- `ai-platform/server/lib/codebase-analyzer.cjs` (127)
- `simplebeacon-vscode-merged/src/analyzers/workspaceAnalyzer.ts` (93)
- `simplebeacon-vscode-merged/src/welcomeDashboardHtml.ts` (67)

**Action:** Review top 10 files. Convert legitimate TODOs to GitHub issues. Remove stale ones. Most are likely intentional backlog markers.

### SB-AI-008: Broad exception catch (2,514 low)

**Status:** Low priority — `catch(e)` or `catch(err)` patterns
**Top files:**

- `simplebeacon-vscode-merged/src/dataServer.ts` (65)
- `ai-platform/server/lib/hsm-adapter/base-adapter.cjs` (55)
- `ai-platform/server/routes/flexible-analyze-api.cjs` (43)

**Action:** Review top files. Where broad catches hide specific errors, narrow to typed catches. Many are intentional top-level error handlers.

### SB-AI-004: Empty exception handler (1,221 medium)

**Status:** Medium priority — empty catch blocks swallow errors
**Top files:** Need investigation

**Action:** This is the same class of issue we already fixed in `sandbox-runner.cjs` and `simplebeacon-plugin.cjs`. Audit top files and add error logging where appropriate.

### SB-AI-010: Wildcard import (368 low)

**Status:** Low priority — `import * as` patterns
**Top file:** `simplebeacon-vscode-merged/src/welcomeDashboardHtml.ts` (5)

**Action:** Replace wildcard imports with named imports where feasible. Low impact.

### SB-AI-001: Debug print statement (185 low)

**Status:** Low priority — `console.log` / `echo` in non-JS files
**Top file:** `start-dashboard.sh` (84)

**Action:** Remove debug prints from shell scripts. Replace with proper logging.

### SB-AI-009: Hardcoded filesystem path (174 low)

**Status:** Low priority — absolute paths in code
**Action:** Replace with config-driven paths or `path.join()`. Low impact.

### SB-AI-005: Eval/exec/Function constructor (66 medium)

**Status:** Medium priority — security concern
**Top file:** `simplebeacon-vscode-merged/vsix-staging/media/d3.v7.min.js` (5485)

**Action:** Most are in third-party minified libs (d3.v7.min.js). Exclude `vsix-staging/` and minified vendor files from scan scope. Audit any in our own source.

### SB-AI-006: Hardcoded credential assignment (39 medium)

**Status:** Medium priority — security concern
**Top file:** `worker-deploy/upload-oracle-to-r2.cjs` (46)

**Action:** Already partially fixed (R2 account ID removed). Audit remaining 39 findings. Most are likely test fixtures or placeholder values.

### SB-AI-015: Wildcard CORS/permissions (22 medium)

**Status:** Medium priority — security concern
**Top file:** `worker-deploy/src/worker.js` (686)

**Action:** Review CORS policies. Restrict to known origins in production.

### SB-AI-014: Long sleep in production code (11 low)

**Status:** Low priority — potential race condition indicator
**Action:** Review and replace with proper async synchronization.

### SB-AI-007: Debug mode enabled in config (4 medium)

**Status:** Medium priority — may expose sensitive info
**Top file:** `packages/simplebeacon-cli/src/rules/universal-ai-rules.json` (155)

**Action:** These are likely scanner rule definitions, not app config. Verify and suppress if false positive.

### SB-AI-013: Disabled authentication check (3 medium)

**Status:** Medium priority — security concern
**Top file:** `packages/simplebeacon-cli/src/rules/agency-handoff-patterns.js` (69)

**Action:** These are likely scanner rule definitions, not app code. Verify and suppress if false positive.

### SB-AI-003: Disabled SSL/TLS verification (1 medium)

**Status:** Medium priority — security concern
**Top file:** `packages/simplebeacon-cli/src/rules/universal-ai-rules.json` (29)

**Action:** Likely a scanner rule definition. Verify and suppress if false positive.

### api-contract (1 low)

**Status:** Low priority
**File:** `ai-platform/scripts/watch-restart-prism.js` (2)

**Action:** Review the OpenAPI contract reference. Likely intentional.

## Recommended actions

### 1. Exclude third-party code from scan scope (highest impact)

Add `node_modules_host/`, `vsix-staging/`, and minified vendor files to the scan exclusion config. This removes ~1,031 findings immediately.

### 2. Fix empty exception handlers (SB-AI-004, 1,221 medium)

This is the highest-impact medium-severity rule. Audit top files and add error logging, following the same pattern used in `sandbox-runner.cjs` and `simplebeacon-plugin.cjs`.

### 3. Suppress scanner rule definition false positives (SB-AI-007, SB-AI-013, SB-AI-003)

The scanner is flagging its own rule definitions. Add `packages/simplebeacon-cli/src/rules/` to the ignore config.

### 4. Triage TODO/FIXME markers (SB-AI-002, 5,316 low)

Review top 10 files. Convert legitimate TODOs to GitHub issues. Remove stale ones.

### 5. Review CORS and credential findings (SB-AI-015, SB-AI-006)

Security-relevant. Audit and restrict where production code is affected.

### 6. Narrow broad exception catches (SB-AI-008, 2,514 low)

Lower priority. Review top files and narrow to typed catches where specific errors are expected.
