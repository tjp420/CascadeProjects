Fix api-contract false positives on OpenAPI infra files

The api-contract rule in comprehensive-scanner.js was flagging 5 LOW
findings on files that ARE the API contract or its testing infrastructure:

  1. ai-platform/api/openapi.yaml:1          — the OpenAPI spec itself
  2. .github/workflows/ai-platform-openapi-prism.yml:62        — CI that lints the spec
  3. .github/workflows/ai-platform-openapi-prism-dispatch.yml:29 — CI dispatch workflow
  4. ai-platform/docker-compose.prism.yml:8  — Prism mock server compose file
  5. ai-platform-openapi-prism.yml:62         — same workflow, different path resolution

These are all legitimate uses of "openapi" — the rule was designed to
flag source code that references openapi/swagger without having a
proper contract, not to flag the contract files themselves or the CI
infrastructure that tests them.

Fix: Added api-contract exclusion block to shouldSkipCheckForPath()
in comprehensive-scanner.js that skips:
  - Files named openapi/swagger .yaml/.yml/.json (the contract definitions)
  - .github/workflows/ directory (CI infrastructure)
  - docker-compose*.yml files (container infrastructure)
  - .spectral.yaml/.yml/.json files (OpenAPI linting rulesets)

Verified: Rule still flags source code (.js) that references openapi
terms, but now correctly skips the 5 false-positive infra files.

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
