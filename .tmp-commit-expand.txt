feat: add inline row expansion to realtime issues table

Makes issue rows in the live analysis stream panel clickable to
expand/collapse and show the raw JSON chunk payload inline.

AnalyzeView.js changes:
- Added stable row IDs: rt-row-{chunkIdx}-{issueIdx} for deterministic
  expansion tracking across re-renders
- Added _realtimeExpanded Set on view instance to track expanded rows
  (multiple rows can be expanded simultaneously)
- Each issue row now has cursor:pointer and a ▼/▲ indicator in the
  Chunk column
- Clicking a row toggles a detail row beneath it that shows:
  - Full issue JSON: type, severity, category, message
  - Chunk metadata: method, confidence, processingTime, recommendations
  - Pretty-printed with <pre> for readability, max-height 300px with scroll
- All JSON output is HTML-escaped (escapeHtml) to prevent XSS
- Expanded state survives re-renders (new chunks don't collapse rows)
- Filter/sort handlers are separate from row click handlers — no interference
- Expanded state cleared on toggle off (this._realtimeExpanded = new Set())

QA: test_plan.md + software_health_report.md per QA framework.
All Level 1/2/3 checks pass. Gate scan PASS. Integration test 16/16.

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
