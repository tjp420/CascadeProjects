feat: add route parameter validation middleware — defense-in-depth for 17 endpoints

Adds a shared validateParam middleware to enforce format validation on
17 route parameters across 7 route files. This is a defense-in-depth
quality improvement, not a security fix — the investigation confirmed
all params are used safely (parameterized SQL or object key lookups).

New file:
- server/middleware/validate-params.cjs (53 lines)
  - validateParam(paramName, pattern) — Express middleware factory
  - VALIDATION_PATTERNS — canonical regex patterns for each param type
  - Returns 400 with { error: 'invalid_parameter', message, paramName, received }
    when validation fails

Validation patterns applied:
- orgId: ^[a-zA-Z0-9_-]{1,100}$ (alphanumeric slug)
- partnerId: ^wl-[a-f0-9]{8}$ (wl- + 8 hex chars)
- configId: ^int-[a-f0-9]{8}$ (int- + 8 hex chars)
- providerId: ^sso-[a-f0-9]{8}$ (sso- + 8 hex chars)
- userId: ^[a-zA-Z0-9_-]{1,100}$ (user ID slug)
- provider: ^[a-z]{3,20}$ (lowercase provider name)
- uuid: ^[a-f0-9-]{36}$ (standard UUID)

Endpoints validated (17 total):
- analytics-routes.cjs: 1 endpoint (orgId) — already committed
- whitelabel-routes.cjs: 6 endpoints (partnerId + orgId)
- integration-routes.cjs: 3 endpoints (configId)
- sso-config-routes.cjs: 4 endpoints (orgId + providerId)
- admin-api.cjs: 2 endpoints (userId)
- sso-routes.cjs: 2 endpoints (provider)
- workspaces.cjs: 3 endpoints (uuid)

Security assessment:
- No SQL injection or path traversal vulnerabilities found
- workspaces.cjs SQL injection flag was a FALSE POSITIVE — all queries
  use parameterized $1/$2 placeholders (immune to injection)
- All params used as object keys or parameterized SQL values
- This commit adds defense-in-depth: fail fast on malformed input,
  better error messages, API contract consistency

Behavioral verification (live server on port 58000):
- Valid orgId (acme-financial) → 404 (valid format, org not found)
- Path traversal (../../etc/passwd) → 400 invalid_parameter
- XSS payload (<script>alert(1)</script>) → 400 invalid_parameter
- Integration test 16/16 pass (no false rejections)

QA: test_plan.md + software_health_report.md per QA framework.
All Level 1/2/3 checks pass. Gate scan PASS. Integration test 16/16.

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
