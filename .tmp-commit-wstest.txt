test: add WebSocket integration test for realtime analysis API

Adds tests/integration/realtime-ws-integration.test.cjs — a standalone
integration test that verifies the full realtime analysis flow against
a live server:

1. GET /api/health — server health check
2. POST /api/realtime/session — create analysis session
3. WebSocket connect to ws://localhost:8082/api/realtime/stream?sessionId=...
4. Receive initial status message
5. Send ping, verify pong round-trip
6. Close WebSocket cleanly
7. DELETE /api/realtime/session/:id — cleanup
8. WebSocket rejection without sessionId (code 1008)

All 16 assertions pass against a running server instance.

Also adds restart-simplebeacon-server.bat (gitignored, local convenience)
that kills stale PIDs on ports 58000/8082 before booting a fresh instance.

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
